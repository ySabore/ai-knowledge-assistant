[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && uvicorn ai_knowledge_assistant.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
