# Implementation backlog

| Item | Notes |
|------|--------|
| Clerk JWT in `auth.py` | Replace dev bearer for production |
| Chat UI | Full messages + sources in `apps/web` |
| Ingestion UX | Show job status from `ingestion_jobs` |
| Observability | Structured logs, request IDs (middleware exists), optional metrics |
| File upload API | If needed beyond path-based ingest; enforce `MAX_FILE_SIZE_MB` |

Pull items into your issue tracker with acceptance criteria per org policy.
