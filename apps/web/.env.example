# Must match API DEV_BEARER_TOKEN when using /me/* from the browser
VITE_DEV_BEARER_TOKEN=dev-local-token
# Optional: Clerk JWT template name. Leave blank to use the standard Clerk session token.
# VITE_CLERK_JWT_TEMPLATE=
